---
description: Mnemonic shortcut to /skill-creator — create or improve a Claude Code skill
argument-hint: (optional) description of the skill to create or improve
---

The user wants to create or improve a Claude Code skill via the upstream `skill-creator` skill. Follow the instructions in `$HOME/.claude/skills/b7s-create-skill/SKILL.md`: invoke `skill-creator` immediately, passing `$ARGUMENTS` as initial context. If `$ARGUMENTS` is empty, invoke `skill-creator` without context — it will interview the user.
