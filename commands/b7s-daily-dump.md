---
description: Alias of /b7s-brainstorm — daily freestyle dump
argument-hint: (optional) starting topic or thought
---

The user invoked the `daily-dump` alias. Read and follow the instructions in `$HOME/.claude/skills/b7s-brainstorm/SKILL.md` exactly — there is no separate behavior. Treat `$ARGUMENTS` as the user's first thought (if any). All conversation and the resulting file are in **Mexican Spanish**.
