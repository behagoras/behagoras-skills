---
description: Alias of /b7s-brainstorm — info dump (sacar todo lo que tengo en la cabeza)
argument-hint: (optional) starting topic or thought
---

The user invoked the `info-dump` alias. Read and follow the instructions in `$HOME/.claude/skills/b7s-brainstorm/SKILL.md` exactly — there is no separate behavior. Treat `$ARGUMENTS` as the user's first thought (if any). All conversation and the resulting file are in **Mexican Spanish**.
