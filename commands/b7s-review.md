---
description: Review brainstorm TODOs and move files between statuses (inbox → in-progress → completed/archived)
argument-hint: (optional) date or slug to scope the review
---

The user wants to review pending brainstorm TODOs. Read and follow the instructions in `$HOME/.claude/skills/b7s-review/SKILL.md` exactly.

If `$ARGUMENTS` is set, scope the review to that file (date `YYYY-MM-DD` or `YYYY-MM-DD-slug`); otherwise review every active file in `.brainstorms/inbox/` and `.brainstorms/in-progress/`. The conversation runs in **Mexican Spanish**.
