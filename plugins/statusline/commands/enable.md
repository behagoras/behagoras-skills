---
description: Enable the Behagoras statusline in user settings
---

Run this command to enable the statusline for the current user:

```bash
node "${CLAUDE_PLUGIN_ROOT}/scripts/enable-statusline.mjs"
```

After the command finishes, tell the user that Claude Code reloads status line settings after the next interaction, and that restarting the session is the cleanest confirmation path.
