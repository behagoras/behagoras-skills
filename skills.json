{
  "$schema": "./skills.schema.json",
  "skills": [
    {
      "name": "b7s-ask",
      "description": "Re-ask the questions Claude just posed in plain text via the structured AskUserQuestion UI — synthesizes 2-4 options per question from the conversation context. Spanish-default, English when the conversation is in English.",
      "path": "b7s-ask",
      "commands": [
        "b7s-ask"
      ],
      "requires": {
        "binaries": []
      },
      "prompts": []
    },
    {
      "name": "b7s-brainstorm",
      "description": "Freestyle brainstorm capture (Mexican Spanish) — listen, organize faithfully, save to .brainstorms/inbox/, deepen via AskUserQuestion.",
      "path": "b7s-brainstorm",
      "commands": [
        "b7s-brainstorm"
      ],
      "requires": {
        "binaries": []
      },
      "prompts": []
    },
    {
      "name": "b7s-create-skill",
      "description": "Scaffold and iterate new skills inside the local behagoras-skills clone using the skill-creator methodology — runs the full interview/draft/eval loop, updates skills.json + commands + README + package.json, validates the manifest, runs pnpm lint, commits atomically, and opens a PR with the chosen release label.",
      "path": "b7s-create-skill",
      "commands": [
        "b7s-create-skill"
      ],
      "requires": {
        "binaries": []
      },
      "prompts": []
    },
    {
      "name": "b7s-prioritize",
      "description": "Rank executable prompts by urgency, dependencies, and TODO coverage — closes the b7s trio (brainstorm → review → prioritize). Read-only.",
      "path": "b7s-prioritize",
      "commands": [
        "b7s-prioritize"
      ],
      "requires": {
        "binaries": []
      },
      "prompts": []
    },
    {
      "name": "b7s-review",
      "description": "Review brainstorm TODOs and move files between statuses (inbox/in-progress/completed/archived). Pairs with b7s-brainstorm.",
      "path": "b7s-review",
      "commands": [
        "b7s-review"
      ],
      "requires": {
        "binaries": []
      },
      "prompts": []
    },
    {
      "name": "video-transcript",
      "description": "Extract transcripts from any video URL (YouTube, Reels, TikTok, Vimeo, ...).",
      "path": "video-transcript",
      "commands": [
        "youtube-transcript",
        "instagram-transcript",
        "tiktok-transcript",
        "video-transcript"
      ],
      "requires": {
        "binaries": [
          "yt-dlp",
          "ffmpeg",
          "python3"
        ],
        "optional": [
          {
            "binary": "mlx_whisper",
            "platforms": [
              "darwin"
            ],
            "install": "pipx install mlx-whisper"
          },
          {
            "binary": "whisper-ctranslate2",
            "platforms": [
              "linux"
            ],
            "install": "pipx install whisper-ctranslate2"
          }
        ]
      },
      "prompts": [
        {
          "key": "vault_dir",
          "question": "Where should --note save vault notes? (relative paths anchor to .transcriptsrc location)",
          "type": "select",
          "choices": [
            {
              "label": "./.transcripts (per-project, gitignored)",
              "value": "./.transcripts"
            },
            {
              "label": "./spider (matches the spider-openclaw-server pattern)",
              "value": "./spider"
            },
            {
              "label": "<obsidian-autodetect>",
              "value": "<obsidian-autodetect>"
            },
            {
              "label": "Custom path…",
              "value": "<custom>"
            }
          ],
          "default": "./.transcripts",
          "writes": {
            "file": ".transcriptsrc",
            "key": "vault_dir"
          }
        },
        {
          "key": "default_with_timestamps",
          "question": "Always emit per-cue timestamps?",
          "type": "confirm",
          "default": false,
          "writes": {
            "file": ".transcriptsrc",
            "key": "default_with_timestamps"
          }
        },
        {
          "key": "default_force_audio",
          "question": "Always skip captions and use Whisper audio path?",
          "type": "confirm",
          "default": false,
          "writes": {
            "file": ".transcriptsrc",
            "key": "default_force_audio"
          }
        }
      ]
    },
    {
      "name": "orchestrate-personal",
      "description": "Route work on the personal machine between Claude and Codex for the best quality-price fit across both quotas. Use when David asks to run a task or saved prompt without naming which model, wants work delegated to the background, asks which model should handle something, or when one model's output needs review by the other. Also reached by run-prompt before executing a spec.",
      "path": "orchestrate-personal",
      "commands": [
        "orchestrate-personal"
      ],
      "requires": {
        "binaries": []
      },
      "prompts": []
    },
    {
      "name": "orchestrate-work",
      "description": "Route work-machine tasks between free Gemini and metered Claude to cut Claude token spend. Use when a task needs anything from the web (Claude has no web access on this machine), when delegating analysis, tests, or routine implementation to Gemini as the cheap executor, when deciding how to review or close a Gemini result, or when a Gemini attempt failed and escalation to Claude is in question.",
      "path": "orchestrate-work",
      "commands": [
        "orchestrate-work"
      ],
      "requires": {
        "binaries": [
          "python3"
        ]
      },
      "prompts": []
    }
  ]
}
